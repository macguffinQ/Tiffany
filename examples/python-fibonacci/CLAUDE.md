# Project Instructions

- Use Python standard library only.
- Keep functions small and typed.
- Run `python3 -m unittest discover -s tests` before reporting completion.
- Do not add package managers or generated files.
